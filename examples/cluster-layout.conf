# Cluster layout configuration for multi-system delegation.
# Each line defines a system with its queue and node allocation pattern.
# Format: <system>:<queue>:<space-separated node counts>

SOURCE_NODES=1
TARGET_LAYOUT=(
    "tuolumne:pdebug:1 1"
    "corona:pdebug:1 1"
    "tioga:pdebug:1"
)
