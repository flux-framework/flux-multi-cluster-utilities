# Multi-system layout configuration (Tuolumne + Corona + Tioga).
# Used by job-delegation.sh for three-system demonstrations.
# Format:
#   SOURCE_NODES          nodes for source (top-level) instance on Tuolumne
#   TARGET_LAYOUT         array of "<system>:<queue>:<node-counts>" entries

SOURCE_NODES=1
TARGET_LAYOUT=(
    "tuolumne:pdebug:1 1"
    "corona:pdebug:1 1 1"
    "tioga:pdebug:1 1"
)
