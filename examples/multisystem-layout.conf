# Multi-system layout configuration (Tuolumne + Corona).
# Format:
#   SOURCE_NODES          nodes for source (top-level) instance on Tuolumne
#   TARGET_LAYOUT         array of "<system>:<queue>:<node-counts>" entries

SOURCE_NODES=1
TARGET_LAYOUT=(
    "tuolumne:pdebug:1 1"
    "corona:pdebug:1 1 1"
)
