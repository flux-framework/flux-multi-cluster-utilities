# Single-system layout configuration for examples.
# Format:
#   SOURCE_NODES      number of nodes for the source (top-level) instance
#   NUM_TARGETS       number of target sub-instances
#   TARGET_NODE_COUNTS  space-separated node counts per target

SOURCE_NODES=1
NUM_TARGETS=3
TARGET_NODE_COUNTS="1 1 1"
